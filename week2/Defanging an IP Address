class Solution(object):
    def defangIPaddr(self, address):
        x=str()
        for i in address:
         if i == ".":
            x+= "[.]"
         else:
            x+=i
        return x
        
